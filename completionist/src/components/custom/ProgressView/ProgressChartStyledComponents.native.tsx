import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  progressBar: {
    width: '100%',
		height: 20,
    borderRadius: 5,
		marginBottom: 8,
    overflow: 'hidden',
  },
  progress: {
    height: '100%',
  },
});