import { Dimensions } from 'react-native';
import styled from 'styled-components/native';

export const RequestGameContentInputContainer = styled.View`
  width: ${Dimensions.get('window').width - 32}px;
  height: 200px;
`;