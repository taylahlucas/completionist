import React from 'react';
import { RequestGameContentInputContainer } from './RequestGameContentStyledComponents.native';

const RequestGameContent = () => {
  // TODO: Add button to enable/disable DLC
  return (
    <RequestGameContentInputContainer />
  );
};

export default RequestGameContent;